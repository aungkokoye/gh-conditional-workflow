name: reusable-workflow
on:
    workflow_call:
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy application
              run: echo "Deploying application..."